name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  checks:
    name: Typecheck / lint / unit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: |
            package-lock.json
            packages/opencode-plugin/package-lock.json
            packages/env-server/package-lock.json

      - name: Install root dependencies
        run: npm ci

      - name: Install package dependencies
        run: |
          npm ci --prefix packages/opencode-plugin
          npm ci --prefix packages/env-server

      - name: Typecheck
        run: |
          npx tsc --noEmit -p tsconfig.server.json
          npx tsc --noEmit -p tsconfig.client.json
          npm run typecheck --prefix packages/opencode-plugin
          npm run typecheck --prefix packages/env-server
      - run: npm run lint
      - run: npm test

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: |
            package-lock.json
            packages/opencode-plugin/package-lock.json
            packages/env-server/package-lock.json

      - name: Install root dependencies
        run: npm ci

      - name: Install package dependencies
        run: |
          npm ci --prefix packages/opencode-plugin
          npm ci --prefix packages/env-server

      - run: npm run build
