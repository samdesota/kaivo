{
  "name": "kaivo-desktop",
  "version": "0.0.1",
  "private": true,
  "type": "commonjs",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsup",
    "dev": "npm run build && env -u CC_INSTANCE_ID -u CC_INSTANCE_ROOT -u CC_APP_PORT -u CC_ENV_PORT -u CC_APP_URL -u CC_APP_SQLITE_PATH -u CC_APP_LOG_PATH -u CC_APP_DATA_DIR -u CC_ENV_URL -u CC_ENV_STATE_DIR -u CC_ENV_WORKING_DIR -u CC_ENV_LABEL -u CC_CLIENT_PORT -u CC_CLIENT_URL -u CC_CLIENT_LOG_PATH -u VITE_PORT -u CC_ALLOWED_ORIGINS -u CC_IDENTITY_URL -u CC_HOST -u CC_KIND -u CC_LABEL -u CC_OPENCODE_PLUGIN_PATH -u CC_PORT -u CC_STATE_DIR -u CC_WORKING_DIR NODE_ENV=development electron dist/main.js",
    "dev:managed": "npm run build && env -u CC_INSTANCE_ID -u CC_INSTANCE_ROOT -u CC_APP_PORT -u CC_ENV_PORT -u CC_APP_URL -u CC_APP_SQLITE_PATH -u CC_APP_LOG_PATH -u CC_APP_DATA_DIR -u CC_ENV_URL -u CC_ENV_STATE_DIR -u CC_ENV_WORKING_DIR -u CC_ENV_LABEL -u CC_CLIENT_PORT -u CC_CLIENT_URL -u CC_CLIENT_LOG_PATH -u VITE_PORT -u CC_ALLOWED_ORIGINS -u CC_IDENTITY_URL -u CC_HOST -u CC_KIND -u CC_LABEL -u CC_OPENCODE_PLUGIN_PATH -u CC_PORT -u CC_STATE_DIR -u CC_WORKING_DIR NODE_ENV=development CC_DESKTOP_MANAGE_SERVICES=true electron dist/main.js",
    "package:mac": "npm run build && electron-packager . \"Kaivo\" --platform=darwin --arch=arm64 --out=release --overwrite --app-bundle-id=${KAIVO_BUNDLE_ID:-com.samdesota.kaivo} --helper-bundle-id=${KAIVO_BUNDLE_ID:-com.samdesota.kaivo}.helper --ignore='^/src($|/)' --ignore='^/dist/.*\\.map$' --ignore='^/\\.kaivo($|/)' --ignore='^/release($|/)' && node scripts/sign-macos-app.cjs release/Kaivo-darwin-arm64/Kaivo.app",
    "install:mac": "npm run package:mac && rm -rf '/Applications/Kaivo.app' '/Applications/Zoottle.app' && cp -R 'release/Kaivo-darwin-arm64/Kaivo.app' /Applications/ && node scripts/check-macos-signing.cjs /Applications/Kaivo.app",
    "sign:mac": "node scripts/sign-macos-app.cjs release/Kaivo-darwin-arm64/Kaivo.app",
    "check-signing:mac": "node scripts/check-macos-signing.cjs /Applications/Kaivo.app",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@samdesota/webframe": "^0.1.11",
    "electron": "^42.0.1"
  },
  "devDependencies": {
    "@electron/osx-sign": "^2.4.0",
    "@electron/packager": "^20.0.0",
    "@types/node": "^22.9.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
